<% if @reviews.any? || @created_reviews.any? %>
  <h3>Reviews of me</h3>
    <ul>
      <% @reviews.each do |review| %>
        <li><span style="font-weight:700;text-transform:uppercase;"><%= review.language %></span>, rated  <i><span style="text-transform:uppercase;"><%= user_convert_to_presented_language_level(review.language_level) %></span></i> by <%= link_to review.reviewer.name, user_path(review.reviewer) %></li>
      <% end %>
    </ul>

  <h3>Reviews given by me</h3>
    <ul>
      <% @created_reviews.each do |review| %>
        <li>
          <span style="font-weight:700;text-transform:uppercase;"><%= review.language %></span>, rated  <i><span style="text-transform:uppercase;"><%= user_convert_to_presented_language_level(review.language_level) %></span></i> of
          <%= link_to review.reviewable.name, user_path(review.reviewable) %>
          <%= link_to "Edit", edit_user_review_path(current_user, review) %>
          <%= link_to "Delete", user_review_path(current_user, review), method: :delete, data: {confirm: "Are you sure you want to delete this review"} %>
        </li>
      <% end %>
    </ul>
<% else %>
  <h4>No Reviews to Display</h4>
<% end %>
