<!DOCTYPE html>
<html>
  <%= render 'layouts/header_mail' %>
  <body>
    <h4>Dear <%= @user.name %>,</h4>
    <p>
      <%= @material.owner.name %> has uploaded new material! <%= link_to "View Material", @material_url, class:'mail-btn' %>
    </p>
  </body>
  <%= render 'layouts/footer_mail' %>
</html>
