<!DOCTYPE html>
<html>
  <%= render 'layouts/header_mail' %>
  <body>
    <h4>Dear <%= @user.name %>,</h4>
    <p>
      <%= "The " %> <%= link_to "Smart Jobs", @board_url, class:'mail-btn' %> <%= " board has new posts!" %>
    </p>
  </body>
  <%= render 'layouts/footer_mail' %>
</html>
