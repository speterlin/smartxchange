Dear <%= @user.name %>,
===============================================

<%= image_tag(attachments["#{@interested_user.name}.jpg"].url, alt: "#{@interested_user.name}") %>
  <span style="text-transform:uppercase;font-weight: 700;"> <%= "#{@interested_user.name}" %></span>, <%= "#{@interested_user.title} wants to start #{a_or_an(@interested_user.language)} " %><span style="text-transform:uppercase;font-weight: 700;"><%= "#{@interested_user.language}"%></span><%=" conversation with you" %>
  <%= link_to "View Profile", @url_interested_user, class: 'mail-btn' %>
