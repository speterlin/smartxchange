<!DOCTYPE html>
<html>
  <%= render 'layouts/header_mail' %>
  <body>
    <h4>Dear <%= @user.name %>,</h4>
      <div class="mail-user">
        <div class="mail-user-img">
          <div class="circle">
            <p><%= image_tag(attachments["#{@interested_user.name}.jpg"].url, alt: "#{@interested_user.name}") %></p>
          </div>
        </div>
        <div class="mail-user-text">
          <p><span class="bold-uppercase"> <%= "#{@interested_user.name}" %></span>, <%= "#{@interested_user.title}, wants to start #{a_or_an(@interested_user.language)} " %><span class="bold-uppercase"><%= "#{@interested_user.language}"%></span><%=" conversation with you" %></p>
          <%= link_to "View Profile", @url_interested_user, class: 'mail-btn' %>
        </div>
      </div>
  </body>
  <%= render 'layouts/footer_mail_simple' %>
</html>
