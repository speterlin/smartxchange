Dear <%= @user.name %>,
===============================================

  <% if @notifications > 0  %>
      You have <%= pluralize(@notifications, "new notification") %>. <%= link_to "Log in", @url_login, class:'mail-btn'  %> to see your unread <%= "notification".pluralize(@notifications) %>!
  <% end %>

  smartXchange is proud to have entered its eleventh month since launching!
  Over the past month we have improved the way conversations are displayed (including showing the recipient's profile image instead of the recipient's nationality),
  updated user preferences (removing Dating and adding some new categories),
  and added a field for adding a url to posts and showing the image associated with the link when displayed on the given Board.
