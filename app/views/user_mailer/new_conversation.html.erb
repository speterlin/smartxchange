<!DOCTYPE html>
<html>
  <%= render 'layouts/header_mail' %>
  <body>
    <h4>Dear <%= @user.name %>,</h4>
      <div class="mail-user">
        <div class="mail-user-img">
          <div class="circle">
            <p><%= image_tag(attachments["#{@initiator.name}.jpg"].url, alt: "#{@initiator.name}") %></p>
          </div>
        </div>
        <div class="mail-user-text">
          <p><span class="bold-uppercase"> <%= "#{@initiator.name}" %></span>, <%= "#{@initiator.title}, has started #{a_or_an(@initiator.language)} " %><span class="bold-uppercase"><%= "#{@initiator.language}"%></span><%=" conversation with you" %></p>
          <%= link_to "View Conversation", @chat_room_url, class: 'mail-btn' %>
        </div>
      </div>
  </body>
  <%= render 'layouts/footer_mail_simple' %>
</html>
