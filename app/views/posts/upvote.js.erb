$('#post-<%= @post.id %> .vote-numbers').text("<%= @post.votes.sum(:value) %> (<%= @post.votes.size %>)");

<%= render partial: 'layouts/move_post_up', locals: { up_votes: @up_votes, post: @post }  %>
